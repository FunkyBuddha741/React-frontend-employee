import React from 'react';
import './Header.scss';

const Header = () => {
	return (
		<div className="container">
			<h2 className="title">Employee Management Application</h2>
		</div>
	);
};

export default Header;
